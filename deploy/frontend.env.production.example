# Replace with the published backend origin. Keep this value out of browser
# source control when the corporate platform supplies it as build configuration.
VITE_API_BASE_URL=https://replace-with-backend-host.invalid
VITE_IDENTITY_MODE=trusted_proxy
